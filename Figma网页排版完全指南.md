# Figma 网页排版完全指南

这份教程是为你的个人游戏作品集网站准备的。你的 `index.html` 里，每个项目板块都有一个空白的 `.section-content` 容器，你可以把 Figma 导出的 HTML，或者自己写的简单 HTML，粘贴进去。

核心思路：你在 Figma 里像做拼贴画一样摆图片、标题、说明文字；再把结果导出成网页代码或图片，放进网站。

参考信息：Figma 官方说明它是免费的在线 UI 设计工具；Anima 官方说明其 Figma 插件可导出 HTML/CSS/Tailwind 等代码。具体免费额度和插件功能可能变化，使用前以官网页面为准。

---

## 第一部分：Figma 入门（10分钟上手）

### 1.1 注册与打开

第一步，打开 Figma 官网。

官网地址：

```text
https://www.figma.com/
```

第二步，点击官网上的【Get started】、【Try Figma for free】或类似按钮。

第三步，用邮箱、Google 账号或 Apple 账号注册。

第四步，选择免费套餐即可。你现在只是做个人作品集排版，免费套餐通常足够开始使用。

第五步，推荐直接使用网页版，不需要先下载客户端。

大白话解释：

- **网页版**：直接在浏览器里使用，像打开 Google Docs 一样。
- **客户端**：安装到电脑上的软件，体验更稳定，但不是必须。

推荐你先用网页版，因为最省事。

✅ 检查点：

- 你已经能打开 Figma。
- 你已经注册并登录。
- 你能看到 Figma 的文件首页。

### 1.2 界面认识

第一步，新建一个设计文件。

在首页点击【New design file】或【Design file】。

第二步，看清楚四个主要区域。

文字模拟截图：

```text
┌──────────────────────────────────────────────┐
│ 顶部工具栏：选择、Frame、形状、钢笔、文字等工具 │
├──────────────┬───────────────────┬───────────┤
│ 左侧图层面板  │ 中间画布           │ 右侧属性面板 │
│ Layers       │ 你的设计都在这里摆放 │ Design属性   │
└──────────────┴───────────────────┴───────────┘
```

第三步，理解每个区域的作用。

- **顶部工具栏**：选择工具、Frame 工具、文字工具、矩形工具都在这里。
- **左侧图层面板**：显示你放进去的所有图片、文字、形状。谁在上面、谁在下面，这里能看到。
- **中间画布**：你真正排版的地方。
- **右侧属性面板**：改大小、位置、旋转角度、颜色、圆角、阴影等。

✅ 检查点：

- 你知道左侧是图层。
- 你知道中间是画布。
- 你知道右侧可以改大小、颜色、旋转和阴影。

### 1.3 第一个 Frame（画板）

第一步，理解什么是 Frame。

**Frame** 可以理解成“网页里的一块画板”。你的项目板块 `.section-content` 最大宽度是 900px，所以 Figma 里也建议先做一个 **900px 宽的 Frame**。

第二步，创建 Frame。

点击顶部工具栏里的【Frame】工具。快捷键是 `F`。

第三步，在画布上拖出一个 Frame。

不用拖得很准，后面可以在右侧精确输入尺寸。

第四步，设置宽度。

选中 Frame，在【右侧属性面板】找到尺寸：

```text
W = 宽度
H = 高度
```

把 `W` 改成：

```text
900
```

第五步，设置高度。

建议一开始把 `H` 设成：

```text
3000
```

为什么先设 3000px？

因为你还不知道一个项目介绍最终会有多高。先给足空间，等排完后再把高度裁短。

第六步，给 Frame 命名。

在左侧图层面板里双击 Frame 名字，改成：

```text
Project 1 - White Snake
```

或者：

```text
项目1 白蛇传
```

✅ 检查点：

- 你创建了一个 Frame。
- 它的宽度是 900px。
- 高度可以先是 3000px。
- 你给 Frame 起了名字。

### 1.4 导入我的素材

第一步，准备图片。

把截图、角色图、UI图、手绘装饰、背景纸纹等素材先整理到电脑文件夹。

第二步，把图片拖进 Figma。

直接从电脑文件夹拖到 Figma 画布中间。

第三步，确认图片格式。

Figma 常用支持：

- JPG：适合截图、背景图、照片。
- PNG：适合透明背景的 Logo、角色、装饰。
- SVG：适合矢量图标、简单线稿。
- GIF：可放动图，但导出到网页时不一定适合所有插件。

第四步，缩放图片。

选中图片后，拖动图片四角可以缩放。

第五步，保持原始比例。

按住 `Shift` 再拖动角点，一般可以保持比例。新版 Figma 很多时候默认保持比例，但你仍然可以用 `Shift` 作为保险。

第六步，精确输入尺寸。

选中图片，在【右侧属性面板】输入：

```text
W = 400
H = 260
```

如果锁链图标是锁住的，改宽度时高度会跟着变，比例不容易变形。

✅ 检查点：

- 你已经把至少一张图片拖进 Figma。
- 你会拖动角点缩放。
- 你知道 JPG / PNG / SVG 的基本用途。

---

## 第二部分：像做拼贴画一样排版

### 2.1 移动与排列

第一步，选择工具。

点击顶部工具栏最左侧的【Move / 选择工具】。快捷键是 `V`。

第二步，选中图片。

在画布上点击图片。图片周围出现边框，说明选中了。

第三步，拖动图片。

按住鼠标左键拖动，就能移动。

第四步，用方向键微调。

- 按方向键：每次移动 1px。
- 按住 `Shift` + 方向键：每次移动 10px。

大白话解释：

1px 是极细微调整，10px 是快速挪动。

第五步，让两张图对齐。

拖动图片时，Figma 会出现红色或紫色的智能参考线。看到线时，说明它正在和另一张图的边缘、中心或间距对齐。

第六步，使用右侧位置数值。

选中元素，在【右侧属性面板】看：

```text
X = 距离 Frame 左边多少像素
Y = 距离 Frame 顶部多少像素
```

如果你想两张图顶部完全对齐，可以让它们的 `Y` 值相同。

✅ 检查点：

- 你能拖动图片。
- 你会用方向键微调。
- 你看得懂 X / Y 是位置。

### 2.2 旋转图片

第一步，选中图片。

第二步，把鼠标移动到图片四角附近。

当鼠标变成旋转形状时，按住拖动。

第三步，轻轻旋转。

做拼贴画效果，建议角度不要太大：

```text
-8° 到 +8°
```

第四步，精确输入角度。

选中图片，在【右侧属性面板】找到【Rotation】或角度输入框。

输入：

```text
-4
```

代表逆时针旋转 4 度。

输入：

```text
5
```

代表顺时针旋转 5 度。

拼贴建议：

- 主截图：-2° 到 +2°，保持稳。
- 小便签/装饰图：-8° 到 +8°，更活泼。
- 文字正文：尽量不要旋转，招生官阅读会累。

✅ 检查点：

- 你会鼠标旋转图片。
- 你会在右侧输入精确角度。
- 你知道不要把正文旋转太多。

### 2.3 图层叠加

第一步，看左侧图层面板。

左侧面板越上面的图层，在画布上通常越靠前。

第二步，调整上下顺序。

选中图片后，可以右键：

- 【Bring to front】放到最前。
- 【Send to back】放到最后。

也可以在左侧图层面板里直接拖动图层上下顺序。

第三步，用快捷键调整。

常见快捷键：

```text
Ctrl + ]   向上一层
Ctrl + [   向下一层
```

第四步，临时隐藏图层。

在左侧图层面板中，找到图层右侧的眼睛图标，点击即可隐藏/显示。

用途：

当你图片太多看花了，可以先隐藏某些装饰，专心调整主图。

✅ 检查点：

- 你知道左侧图层越上越靠前。
- 你会把图片放到前面或后面。
- 你会隐藏图层。

### 2.4 添加文字

第一步，选择文字工具。

点击顶部工具栏的【Text / 文字】。快捷键是 `T`。

第二步，在 Frame 里点击。

直接输入标题，例如：

```text
《白蛇传》
```

第三步，输入正文。

再次按 `T`，在标题下面拖出一个文本框，然后输入说明文字。

第四步，调整字体。

选中文字，在【右侧属性面板】找到字体名称。

适合网页的字体：

- `Varela Round`：英文圆润，适合正文。
- `ZCOOL KuaiLe`：中文手写风，适合标题。
- `Ma Shan Zheng`：毛笔字风格，适合《白蛇传》标题或装饰字。

重要提醒：

如果你在 Figma 用了一个电脑里才有、网页代码没引入的字体，网页上可能显示不出来。所以标题、正文尽量使用你代码里已经引入的字体。

你的网站当前引入了：

```text
ZCOOL KuaiLe
Ma Shan Zheng
Varela Round
```

第五步，调整字号。

建议：

- 大标题：48-72px。
- 小标题：28-40px。
- 正文：16-20px。
- 图片说明：14-16px。

第六步，调整颜色。

选中文字，在【右侧属性面板】找到【Fill / 填充】颜色。

推荐：

```text
深色正文：#2f2a33
灰色说明：#776d7a
粉色点缀：#f2a7b0
宣纸底色：#f8f2df
```

第七步，调整行距。

选中文字，在【右侧属性面板】找到【Line height / 行高】。

正文建议：

```text
150% 到 170%
```

✅ 检查点：

- 你会添加标题。
- 你会添加正文。
- 你知道网页字体最好和代码里引入的字体一致。

### 2.5 装饰效果

第一步，给图片添加阴影。

选中图片，在【右侧属性面板】找到【Effects】。

点击【+】添加效果，选择【Drop shadow】。

建议数值：

```text
X = 0
Y = 12
Blur = 24
Opacity = 15%
```

第二步，添加圆角。

选中图片，在【右侧属性面板】找到圆角输入框。

建议：

```text
16 到 28
```

第三步，调整透明度。

选中元素，在【右侧属性面板】找到【Opacity】。

比如墨迹装饰可以设成：

```text
30% 到 60%
```

第四步，添加边框。

选中图片，在【右侧属性面板】找到【Stroke】。

点击【+】，设置：

```text
颜色：#ffffff
粗细：4
```

这样图片会像贴纸一样。

✅ 检查点：

- 你会添加阴影。
- 你会添加圆角。
- 你会调透明度。
- 你会做贴纸边框。

### 2.6 背景设置

第一步，选中整个 Frame。

不要选中里面的图片，要点 Frame 的边缘，或者在左侧图层面板点击 Frame 名字。

第二步，在【右侧属性面板】找到【Fill】。

第三步，设置背景色。

项目1宣纸色建议：

```text
#f8f2df
```

项目2/3奶油色建议：

```text
#fef9ef
```

第四步，设置背景图。

把背景图拖进 Frame，然后右键选择【Send to back】。

第五步，适配背景大小。

选中背景图，拖大到覆盖整个 Frame。

如果图片被拉变形，按住 `Shift` 缩放，保持比例。

✅ 检查点：

- 你会选中 Frame。
- 你会改 Frame 背景色。
- 你会把背景图放到最底层。

---

## 第三部分：针对我的网站项目的具体操作

### 3.1 项目1《白蛇传》排版建议

第一步，创建 900px 宽 Frame。

建议：

```text
W = 900
H = 3000
```

第二步，设置水墨风背景。

选中 Frame，在【右侧属性面板】的【Fill】里输入：

```text
#f8f2df
```

这是一种接近宣纸的底色。

第三步，处理毛笔字标题。

方法 A：用图片。

优点：

- 最稳定。
- 可以用你自己写的字或设计好的字。
- 网页显示不会受字体影响。

缺点：

- 之后改字要重新导出图片。

方法 B：用文字工具。

优点：

- 改文字方便。
- 导出代码后仍然是文字。

缺点：

- 如果网页没有对应字体，显示可能不同。

推荐：

《白蛇传》这种标题可以用图片，正文用文字。

第四步，摆放墨迹装饰。

建议：

- 墨迹放在角落，不要挡正文。
- 墨迹透明度设 25%-50%。
- 主视觉周围留白，体现水墨感。
- 不要每个角都放装饰，会显得拥挤。

第五步，招生官阅读重点。

建议在项目1里包含：

- 项目名称。
- 项目类型。
- 你的职责。
- 游戏机制。
- 设计过程。
- 玩家测试或迭代。
- 最终成果截图。

✅ 检查点：

- 项目1 Frame 宽度是 900px。
- 背景是宣纸色或水墨图。
- 标题、截图、说明文字都放好了。

### 3.2 项目2/3 排版建议

第一步，保持 900px 宽。

项目2和项目3也建议：

```text
W = 900
H = 2500 到 3500
```

第二步，使用童趣手绘配色。

推荐颜色：

```text
奶油底：#fef9ef
粉色：#f2a7b0
浅蓝：#b8d8f8
浅绿：#cdeac0
浅紫：#d9c7f4
```

第三步，营造手工拼贴感。

具体做法：

第一张截图：

```text
Rotation = -2°
Drop shadow: Y 12, Blur 24, Opacity 15%
```

第二张截图：

```text
Rotation = 3°
Drop shadow: Y 10, Blur 20, Opacity 12%
```

第三张小装饰：

```text
Rotation = -7°
Opacity = 80%
```

第四步，给图片加白边。

选中图片，添加【Stroke】：

```text
颜色：#ffffff
粗细：4
```

这样会像手账贴纸。

第五步，保持阅读清楚。

视觉可以自由，但文字介绍要规整：

- 标题不要太小。
- 正文不要旋转。
- 一段文字不要超过 6 行。
- 图片之间留出呼吸空间。

✅ 检查点：

- 每张图都有轻微不同角度。
- 图片有阴影或白边。
- 正文仍然容易阅读。

### 3.3 个人简介板块排版建议

第一步，头像 + 文字布局。

推荐布局：

```text
左侧：圆形头像
右侧：姓名、头衔、个人陈述
```

第二步，技能标签。

技能标签像小贴纸一样排列：

```text
Unity    Unreal    C#    Level Design    Figma
```

每个标签建议：

- 圆角大一点。
- 背景用白色或浅色。
- 文字不要太长。

第三步，时间线。

时间线可以左侧年份、右侧描述：

```text
2026   项目名称
       你的职责和成果
```

第四步，申请用途提醒。

面向美国研究生招生官时，个人简介不要只写“我很热爱游戏”。更建议写：

- 你的设计兴趣是什么。
- 你用什么方法做设计。
- 你如何测试和迭代。
- 你为什么适合研究生项目。

✅ 检查点：

- 简介能体现你的专业目标。
- 技能标签清晰。
- 时间线能看出成长路径。

---

## 第四部分：导出为网页代码（核心步骤）

### 4.1 准备工作

第一步，整理图层名字。

在左侧图层面板里，把图层改成英文或简单名字：

```text
title
main-screenshot
note-card
character-art
```

不要用：

```text
Rectangle 123
Image 84
Group 999
```

第二步，检查图片命名。

导出到网站时，最终图片建议放到：

```text
images/
```

第三步，检查文字。

确认文字没有错别字。尤其是英文申请作品集，建议让别人帮你校对。

第四步，知道哪些效果可能导出不完美。

可能出现差异：

- 复杂阴影。
- 模糊效果。
- 旋转后的响应式布局。
- Figma 特有混合模式。
- 字体不一致。

大白话解释：

Figma 是设计工具，网页是代码工具。插件很聪明，但不能 100% 把设计变成完美网页。

✅ 检查点：

- 图层命名清楚。
- 图片路径计划好了。
- 你知道导出后可能需要手动微调。

### 4.2 安装插件

第一步，打开 Figma 文件。

第二步，点击顶部或左上角【Resources / 资源】图标。

第三步，切换到【Plugins / 插件】。

第四步，在搜索框输入插件名称。

推荐比较：

| 插件 | 优点 | 缺点 | 适合你吗 |
|---|---|---|---|
| Anima | 官方文档明确支持 Figma 转 HTML/CSS/React 等；功能完整 | 一些导出功能可能需要账号或付费；代码可能较复杂 | 推荐优先试 |
| Figma to HTML | 名字直接，通常操作简单 | 插件质量和维护情况差异大 | 可作为备选 |
| html.to.design | 更偏把网页导入 Figma，不是你的主要需求 | 方向相反 | 暂不推荐 |

第五步，推荐你先试 Anima。

原因：

- 它有较完整的导出流程。
- 对设计师更友好。
- 如果免费额度够用，最省心。

第六步，安装插件。

搜索到插件后，点击【Run】或【Install】。

如果 Figma 要你授权，按提示登录插件账号。

✅ 检查点：

- 你能打开插件搜索。
- 你安装或运行了 Anima。
- 你知道如果 Anima 不顺手，可以试 Figma to HTML。

### 4.3 导出操作

第一步，选中你要导出的 Frame。

一定要选中整个 Frame，不是只选中某张图片。

第二步，打开插件。

点击【Resources / 资源】→【Plugins / 插件】→ 选择【Anima】或你的导出插件。

第三步，选择导出类型。

如果插件给你选项，优先选择：

```text
HTML + CSS
```

不要优先选 React，因为你的网站是普通 HTML 文件。

第四步，选择固定宽度。

如果有响应式选项，作为新手推荐先选：

```text
Fixed width
```

原因：

你的 `.section-content` 本来就是 900px 宽的容器，固定宽度更容易粘进去。

第五步，导出。

插件可能给你：

- 一段 HTML。
- 一个 ZIP 文件。
- 一个网页预览链接。

第六步，保存导出结果。

如果是 ZIP，解压后看里面通常有：

```text
index.html
style.css
images/
```

你需要的是里面的某一段 HTML 和对应图片，不一定要整个文件。

✅ 检查点：

- 你选中了整个 Frame。
- 导出格式优先是 HTML + CSS。
- 你知道导出后可能得到代码、文件夹或 ZIP。

### 4.4 粘贴到我的网站

第一步，打开你的项目文件。

打开：

```text
C:\Users\25973\OneDrive\Desktop\MyGamePortfolio\index.html
```

第二步，找到项目内容区。

🔍 在代码中搜索：`🔧 在此粘贴 Figma 导出的 HTML 代码`

你会看到类似：

```html
<div class="section-content">
    <!-- 🔧 在此粘贴 Figma 导出的 HTML 代码，或自由排版 -->
</div>
```

第三步，把导出的 HTML 粘进去。

修改前：

```html
<div class="section-content">
    <!-- 🔧 在此粘贴 Figma 导出的 HTML 代码，或自由排版 -->
</div>
```

修改后示例：

```html
<div class="section-content">
    <div class="figma-project project1-layout">
        <h2>《白蛇传》</h2>
        <img src="images/project1-main.png" alt="《白蛇传》主视觉">
        <p>这是项目介绍文字。</p>
    </div>
</div>
```

第四步，不要重复粘贴完整网页结构。

如果插件导出里有这些内容，不要整段放进 `.section-content`：

```html
<!DOCTYPE html>
<html>
<head>
<body>
```

你只需要 `<body>` 里面和项目有关的那一小段。

第五步，处理 CSS。

如果插件导出 CSS，有两种简单做法：

推荐方法：把 CSS 复制到你当前 `index.html` 里面的 `<style>` 标签末尾。

🔍 在代码中搜索：`</style>`

在 `</style>` 前面粘贴插件 CSS。

第六步，处理图片路径。

Figma 或插件导出的图片可能是：

```html
<img src="https://...临时链接...">
```

或者：

```html
<img src="./img/image1.png">
```

建议你统一改成：

```html
<img src="images/project1-main.png" alt="项目截图">
```

详细步骤：

第一步，把插件导出的图片下载或复制到你的项目：

```text
MyGamePortfolio/images/
```

第二步，改图片名字，例如：

```text
project1-main.png
project1-ui-01.png
project1-map.png
```

第三步，在 HTML 里改路径。

修改前：

```html
<img src="./assets/image-01.png">
```

修改后：

```html
<img src="images/project1-main.png" alt="《白蛇传》游戏截图">
```

第七步，给图片加 alt。

`alt` 是图片说明。图片加载失败时会显示，也有助于网页可访问性。

示例：

```html
<img src="images/project1-main.png" alt="《白蛇传》关卡场景截图">
```

✅ 检查点：

- 你只粘贴了项目相关 HTML，没有粘贴第二个完整网页。
- 图片已经放进 `images/`。
- 图片路径已经改成本地路径。
- CSS 放进了当前 `index.html` 的 `<style>` 里。

### 4.5 验证与调试

第一步，在浏览器打开网页。

双击：

```text
index.html
```

第二步，检查项目板块。

点击导航里的《白蛇传》或项目2/3，看内容是否出现。

第三步，图片位置跑偏怎么办。

先检查：

- 图片路径是否正确。
- CSS 是否粘贴完整。
- 导出的 HTML 是否放在 `.section-content` 内。

第四步，字体显示不对怎么办。

原因通常是网页没有引入这个字体。

解决：

要么把 Figma 字体换成网站已引入字体：

```text
ZCOOL KuaiLe
Ma Shan Zheng
Varela Round
```

要么在 `index.html` 的 Google Fonts 链接里添加新字体。

第五步，移动端显示错乱怎么办。

作为新手，最简单方案：

在 CSS 里给 Figma 导出的整体容器加：

```css
.figma-project {
    max-width: 100%;
    overflow: hidden;
}

.figma-project img {
    max-width: 100%;
    height: auto;
}
```

如果插件导出大量绝对定位，手机端可能仍然不理想。这时可以考虑“截图法”，见第五部分。

✅ 检查点：

- 你能在浏览器看到新内容。
- 图片能显示。
- 字体和布局基本正常。
- 手机端至少没有横向撑爆页面。

---

## 第五部分：高效工作流总结

### 5.1 推荐工作流程

第一步，在 Figma 里排好一个板块。

只做一个项目，不要三个一起做。先完成项目1。

第二步，导出 HTML。

用 Anima 或 Figma to HTML 导出。

第三步，下载图片到 `images/` 文件夹。

命名示例：

```text
project1-main.png
project1-detail-01.png
project1-process.png
```

第四步，修改图片路径为本地路径。

统一使用：

```html
src="images/文件名.png"
```

第五步，粘贴到项目代码中。

🔍 在代码中搜索：`🔧 在此粘贴 Figma 导出的 HTML 代码`

第六步，浏览器预览。

双击 `index.html`。

第七步，不满意就回 Figma 修改。

修改后重新导出，再替换项目对应代码。

✅ 检查点：

- 你一次只处理一个项目板块。
- 图片都放进 `images/`。
- 每次改完都预览。

### 5.2 迭代修改的技巧

第一步，保留 Figma 源文件。

不要只保留导出的 HTML。Figma 源文件是以后继续修改排版的地方。

第二步，如果只换一张图，不一定重新导出。

假设网页里有：

```html
<img src="images/project1-main.png" alt="项目截图">
```

你只需要把 `images/project1-main.png` 替换成新图片，保持文件名不变，网页会自动显示新图。

第三步，如果只改文字，可以直接在 `index.html` 改。

不用回 Figma。

第四步，如果大幅调整布局，回 Figma 改。

比如图片位置、旋转角度、整体拼贴结构都变了，就重新导出。

✅ 检查点：

- 你保存了 Figma 文件。
- 小改动直接改网页。
- 大改动回 Figma。

### 5.3 备选方案

#### 方案 A：手动从 Figma 获取位置信息

适合你想自己写简单 HTML/CSS，但不想猜位置。

第一步，选中 Figma 里的图片。

第二步，看【右侧属性面板】：

```text
X
Y
W
H
Rotation
```

第三步，把这些信息写成 CSS。

示例：

```html
<div class="manual-collage">
    <img class="shot shot-a" src="images/project1-main.png" alt="项目截图">
</div>
```

```css
.manual-collage {
    position: relative;
    min-height: 900px;
}

.shot-a {
    position: absolute;
    left: 80px;
    top: 120px;
    width: 520px;
    transform: rotate(-4deg);
}
```

大白话解释：

- `left` 对应 Figma 的 X。
- `top` 对应 Figma 的 Y。
- `width` 对应 Figma 的 W。
- `rotate` 对应 Figma 的 Rotation。

#### 方案 B：截图法（终极省事方案）

如果插件导出效果不好，就把整个项目板块导出成一张大图。

优点：

- 最像 Figma 原稿。
- 不用处理复杂 HTML。
- 不会出现图片错位。

缺点：

- 文字不是网页文字，招生官不能复制。
- 手机端可能需要缩放。
- SEO 和可访问性较弱。

操作步骤：

第一步，选中整个 Frame。

第二步，在【右侧属性面板】找到【Export】。

第三步，选择：

```text
PNG
2x
```

第四步，点击【Export】。

第五步，保存到：

```text
images/project1-full.png
```

第六步，在网站里粘贴：

```html
<div class="section-content">
    <img class="full-project-image" src="images/project1-full.png" alt="《白蛇传》项目完整排版图">
</div>
```

第七步，在 CSS 里加：

```css
.full-project-image {
    width: 100%;
    height: auto;
    border-radius: 20px;
    display: block;
}
```

推荐用法：

如果你时间紧，先用截图法上线。等以后有时间，再慢慢改成真正 HTML 排版。

✅ 检查点：

- 你知道插件不好用时还有截图法。
- 你知道截图法省事但不够灵活。
- 你知道如何把整块 PNG 放进 `.section-content`。

---

## 最后建议

你的网站面向美国研究生招生官，所以 Figma 排版不要只追求“好看”，还要让人快速看懂你的设计能力。

每个项目板块最好包含：

```text
项目一句话简介
你的职责
核心玩法 / 设计目标
设计过程
迭代证据
最终效果
反思总结
```

最稳的第一版路线：

第一步，用 Figma 做漂亮拼贴。

第二步，把每个项目先用“截图法”放进网站。

第三步，确认整体视觉完整。

第四步，再逐步把重要文字改成真正 HTML。

这样你不会被代码卡住，也能尽快拥有一个能展示的作品集网站。
