# 个人游戏作品集网站填空指南

这份指南对应当前项目里的 `index.html`。你可以一边打开本指南，一边在 VS Code 里编辑 `index.html`。

最重要的操作方法：在 VS Code 按 `Ctrl + F`，输入指南里的搜索关键词，找到对应代码，然后按示例替换。

## 一、文件与文件夹准备

### 1. 推荐文件夹结构

在 `C:\Users\25973\OneDrive\Desktop\MyGamePortfolio` 里建议整理成这样：

```text
MyGamePortfolio/
├─ index.html
├─ style.css
├─ agent.md
├─ 填空指南.md
└─ images/
   ├─ logo-small.png
   ├─ welcome-bg.jpg
   ├─ welcome-logo.png
   ├─ project1-bg.jpg
   ├─ project2-bg.jpg
   ├─ project3-bg.jpg
   ├─ avatar.jpg
   └─ divider.png
```

### 2. 每个文件夹和文件的用途

- `index.html`：网站主文件，当前所有 HTML、CSS、JavaScript 都写在里面。你主要改这个文件。
- `style.css`：旧样式文件。当前新版 `index.html` 没有引用它，可以先保留，不影响网页。
- `agent.md`：给后续 AI 助手看的项目说明。
- `填空指南.md`：你现在正在看的填空说明。
- `images/`：建议你新建这个文件夹，把所有图片都放进去。

### 3. 图片命名建议

推荐用英文小写、短横线命名，避免中文路径导致网页加载问题。

```text
logo-small.png       导航栏左上角小 Logo
welcome-bg.jpg       欢迎页大背景
welcome-logo.png     欢迎页中间 Logo / 名字图
project1-bg.jpg      《白蛇传》水墨背景
project2-bg.jpg      项目2背景
project3-bg.jpg      项目3背景
avatar.jpg           个人头像
divider.png          可选手绘分隔线
```

## 二、图片替换清单（逐项说明）

### 1. 导航栏小 Logo

🔍 在代码中搜索：`Logo 小图`

- 位置：约第 670 行附近，`.nav-logo-img` 里面。
- 用途：页面顶部导航栏左侧的小圆形 Logo。
- 建议尺寸：200×200px 或 160×160px。
- 建议格式：PNG。如果是头像/图标，透明 PNG 最好。
- 注意事项：图片会被裁成圆形，主体最好放在图片中央。

修改前：

```html
<span class="nav-logo-img">
    <!-- 🔧 替换：Logo 小图，示例：<img src="images/logo-small.png" alt="Logo"> -->
</span>
```

修改后：

```html
<span class="nav-logo-img">
    <img src="images/logo-small.png" alt="Logo">
</span>
```

### 2. 欢迎页背景

🔍 在代码中搜索：`欢迎页背景`

- 位置：约第 263-278 行附近，`.welcome-section` 的 CSS。
- 用途：网站首屏大背景。
- 建议尺寸：1920×1080px。
- 建议格式：JPG。插画透明图也可用 PNG。
- 注意事项：文件建议小于 1MB；背景不要太花，否则中间 Logo 和 tagline 不清楚。

修改前：

```css
.welcome-section {
    /* 🔧 替换：欢迎页背景 */
    background-image:
        linear-gradient(rgba(254, 249, 239, 0.72), rgba(254, 249, 239, 0.88)),
        radial-gradient(circle at 24% 38%, rgba(255, 214, 165, 0.56), transparent 28%);
}
```

修改后：

```css
.welcome-section {
    background-image:
        linear-gradient(rgba(254, 249, 239, 0.55), rgba(254, 249, 239, 0.75)),
        url("images/welcome-bg.jpg");
}
```

### 3. 欢迎页 Logo / 名字图片

🔍 在代码中搜索：`Logo 图片`

- 位置：约第 704-708 行附近，`.welcome-logo` 里面。
- 用途：欢迎页中央的大 Logo、名字字样或作品集标题图。
- 建议尺寸：900×360px，或 600×240px。
- 建议格式：PNG，透明背景最好。
- 注意事项：如果放图片，就可以删除或保留原来的文字。最简单是用图片替换文字。

修改前：

```html
<div class="welcome-logo">
    <!-- 🔧 替换：Logo 图片，示例：<img src="images/welcome-logo.png" alt="你的名字"> -->
    <span class="zh">游戏作品集</span>
    <span class="en">Game Design Portfolio</span>
</div>
```

修改后：

```html
<div class="welcome-logo">
    <img src="images/welcome-logo.png" alt="你的名字 Game Design Portfolio">
</div>
```

### 4. 项目1《白蛇传》水墨背景图

🔍 在代码中搜索：`项目1 水墨背景图`

- 位置：约第 393 行附近，`.project-shuimo .section-bg`。
- 用途：项目1板块的全宽背景图。
- 建议尺寸：1920×1080px。
- 建议格式：JPG 或 PNG。
- 注意事项：水墨图可以偏灰、宣纸、留白。不要太暗，否则内容容器不突出。

修改前：

```css
.project-shuimo .section-bg {
    /* 🔧 替换：项目1 水墨背景图 */
    background-image:
        linear-gradient(rgba(248, 242, 223, 0.58), rgba(248, 242, 223, 0.84)),
        radial-gradient(circle at 26% 30%, rgba(32, 32, 32, 0.12), transparent 25%);
}
```

修改后：

```css
.project-shuimo .section-bg {
    background-image:
        linear-gradient(rgba(248, 242, 223, 0.45), rgba(248, 242, 223, 0.78)),
        url("images/project1-bg.jpg");
}
```

### 5. 项目2背景图

🔍 在代码中搜索：`项目2/3 背景图`

- 位置：约第 408 行附近，`.project-handdrawn .section-bg`。
- 用途：项目2和项目3默认共用的手绘风背景。项目3后面有单独覆盖。
- 建议尺寸：1920×1080px。
- 建议格式：JPG。
- 注意事项：项目2背景建议柔和，不要抢作品内容。

修改前：

```css
.project-handdrawn .section-bg {
    /* 🔧 替换：项目2/3 背景图 */
    background-image:
        linear-gradient(rgba(254, 249, 239, 0.62), rgba(254, 249, 239, 0.84)),
        repeating-linear-gradient(-8deg, rgba(255, 214, 165, 0.26) 0 2px, transparent 2px 18px);
}
```

修改后：

```css
.project-handdrawn .section-bg {
    background-image:
        linear-gradient(rgba(254, 249, 239, 0.55), rgba(254, 249, 239, 0.82)),
        url("images/project2-bg.jpg");
}
```

### 6. 项目3背景图

🔍 在代码中搜索：`项目3 背景图`

- 位置：约第 416 行附近，`#project3 .section-bg`。
- 用途：只控制项目3板块背景。
- 建议尺寸：1920×1080px。
- 建议格式：JPG。
- 注意事项：这里会覆盖项目2/3的共用背景，所以项目3要单独在这里改。

修改前：

```css
#project3 .section-bg {
    /* 🔧 替换：项目3 背景图 */
    background-image:
        linear-gradient(rgba(254, 249, 239, 0.64), rgba(254, 249, 239, 0.86)),
        repeating-linear-gradient(7deg, rgba(184, 216, 248, 0.26) 0 2px, transparent 2px 20px);
}
```

修改后：

```css
#project3 .section-bg {
    background-image:
        linear-gradient(rgba(254, 249, 239, 0.55), rgba(254, 249, 239, 0.82)),
        url("images/project3-bg.jpg");
}
```

### 7. 个人头像

🔍 在代码中搜索：`个人头像`

- 位置：约第 765 行附近，`.about-avatar` 里面。
- 用途：个人简介板块左侧圆形头像。
- 建议尺寸：300×300px 或 600×600px。
- 建议格式：JPG 或 PNG。
- 注意事项：图片会变成圆形，脸或主体要居中。

修改前：

```html
<div class="about-avatar">
    <!-- 🔧 替换：个人头像，示例：<img src="images/avatar.jpg" alt="你的头像"> -->
    <span class="zh">头像</span>
    <span class="en">Avatar</span>
</div>
```

修改后：

```html
<div class="about-avatar">
    <img src="images/avatar.jpg" alt="你的头像">
</div>
```

### 8. 手绘分隔线（可选）

🔍 在代码中搜索：`手绘分隔线图片`

- 位置：约第 365-373 行附近，`.section-divider`。
- 用途：项目板块之间的虚线分隔。
- 建议尺寸：900×60px。
- 建议格式：PNG，透明背景最好。
- 注意事项：这是可选项。不替换也可以，当前已经有虚线效果。

修改后示例：

```css
.section-divider {
    background-image: url("images/divider.png");
    background-repeat: no-repeat;
    background-position: center;
    background-size: contain;
    border-top: 0;
}
```

## 三、文字内容替换（逐项说明）

### 1. 导航栏名字

🔍 在代码中搜索：`导航栏名字文字`

- 位置：网页顶部左侧。
- 当前占位文字：`你的名字` / `Your Name`。
- 字数建议：中文 2-8 个字，英文 2-20 个字符比较稳。

修改前：

```html
<span class="zh">你的名字</span>
<span class="en">Your Name</span>
```

修改后：

```html
<span class="zh">李小白</span>
<span class="en">Xiaobai Li</span>
```

### 2. 欢迎页标题 / Logo文字

🔍 在代码中搜索：`游戏作品集`

- 位置：欢迎页中间大框。
- 当前占位文字：`游戏作品集` / `Game Design Portfolio`。
- 字数建议：如果不用图片，中文 4-10 个字，英文不超过 3-4 个词。

修改前：

```html
<span class="zh">游戏作品集</span>
<span class="en">Game Design Portfolio</span>
```

修改后：

```html
<span class="zh">李小白作品集</span>
<span class="en">Xiaobai Li Portfolio</span>
```

### 3. Tagline

🔍 在代码中搜索：`中文 Tagline`

- 位置：欢迎页 Logo 下方。
- 当前占位文字：`用系统、叙事与互动，设计能被玩家记住的体验。`
- 字数建议：中文 15-35 字；英文 8-18 个词。

修改前：

```html
<p class="zh">用系统、叙事与互动，设计能被玩家记住的体验。</p>
<p class="en">Designing memorable play through systems, narrative, and interaction.</p>
```

修改后：

```html
<p class="zh">我用关卡、机制与叙事设计有温度的互动体验。</p>
<p class="en">I design expressive play through levels, systems, and narrative.</p>
```

### 4. 项目导航名称

🔍 在代码中搜索：`项目2`

- 位置：顶部导航栏。
- 当前占位文字：`项目2` / `Project 2`。
- 字数建议：导航项越短越好，中文 2-8 个字，英文 1-3 个词。

修改前：

```html
<a href="#project2" data-target="project2"><span class="zh">项目2</span><span class="en">Project 2</span></a>
```

修改后：

```html
<a href="#project2" data-target="project2"><span class="zh">梦境修理师</span><span class="en">Dream Tuner</span></a>
```

### 5. 个人简介姓名与头衔

🔍 在代码中搜索：`替换：姓名`

- 位置：个人简介板块。
- 当前占位文字：`你的名字` / `Your Name`。
- 字数建议：姓名正常长度即可；头衔建议一行内显示。

修改前：

```html
<span class="zh">游戏设计师 / 互动叙事创作者 / 研究生申请者</span>
<span class="en">Game Designer / Interactive Storyteller / Graduate Applicant</span>
```

修改后：

```html
<span class="zh">游戏设计师 / 关卡设计 / 互动叙事</span>
<span class="en">Game Designer / Level Designer / Interactive Storyteller</span>
```

### 6. 个人陈述

🔍 在代码中搜索：`个人陈述第 1 段`

- 位置：个人简介里的 Statement。
- 当前占位文字：每段以 `【替换为真实内容】` 或 `[Replace with real content]` 开头。
- 字数建议：每段中文 60-120 字，英文 50-90 词。太长会让简介板块显得拥挤。

修改前：

```html
<p class="zh">【替换为真实内容】我关注游戏机制、叙事结构与玩家情感之间的关系...</p>
<p class="en">[Replace with real content] I explore the relationship...</p>
```

修改后：

```html
<p class="zh">我关注玩家如何通过规则理解世界，并尝试把东方民间故事转化为可互动的关卡体验。</p>
<p class="en">I explore how players understand worlds through rules, and I translate folklore into interactive level experiences.</p>
```

## 四、中英文双语设置

### 1. 如何添加中文内容

🔍 在代码中搜索：`class="zh"`

中文内容放在带 `class="zh"` 的标签里。

```html
<p class="zh">这里写中文内容。</p>
```

### 2. 如何添加英文翻译

🔍 在代码中搜索：`class="en"`

英文内容放在带 `class="en"` 的标签里。

```html
<p class="en">Write the English version here.</p>
```

### 3. 只有中文时如何添加英文版本

修改前：

```html
<p class="zh">这是我的项目介绍。</p>
```

修改后：

```html
<p class="zh">这是我的项目介绍。</p>
<p class="en">This is my project description.</p>
```

### 4. 语言切换按钮的工作原理

🔍 在代码中搜索：`语言切换：中文默认显示`

大白话解释：点击 English 时，JavaScript 会给 `<body>` 加上 `lang-en` 这个类；CSS 看到这个类后，就把 `.zh` 隐藏，把 `.en` 显示。

关键代码：

```js
body.classList.toggle("lang-en", selectedLang === "en");
```

对应 CSS：

```css
body.lang-en .zh {
    display: none !important;
}

body.lang-en .en {
    display: inline;
}
```

## 五、导航栏修改

### 1. 修改导航项名称

🔍 在代码中搜索：`nav-links`

项目2示例：

```html
<a href="#project2" data-target="project2"><span class="zh">项目2</span><span class="en">Project 2</span></a>
```

改成：

```html
<a href="#project2" data-target="project2"><span class="zh">梦境修理师</span><span class="en">Dream Tuner</span></a>
```

### 2. 确保点击后跳到正确位置

`href="#project2"` 必须对应下面 section 的 `id="project2"`。

```html
<a href="#project2" data-target="project2">...</a>

<section id="project2" class="project-section project-handdrawn">
```

不要把一个改了另一个没改。

### 3. 添加导航项

最简单方式：复制一个 `<a>`，再复制一个对应的 `<section>`。

导航新增：

```html
<a href="#project4" data-target="project4"><span class="zh">项目4</span><span class="en">Project 4</span></a>
```

还要在 JS 里加入监听：

🔍 在代码中搜索：`observedSections`

修改前：

```js
const observedSections = document.querySelectorAll("#project1, #project2, #project3, #about");
```

修改后：

```js
const observedSections = document.querySelectorAll("#project1, #project2, #project3, #project4, #about");
```

### 4. 删除导航项

删除对应 `<a>`，也建议删除对应 `<section>`。如果只删导航不删 section，网页还能显示，只是不能从导航跳过去。

## 六、项目板块排版（重点）

### 1. 三个项目板块的位置

🔍 在代码中搜索：`id="project1"`

- 项目1：约第 727 行，`id="project1"`，class 有 `project-shuimo`。
- 项目2：约第 738 行，`id="project2"`，class 有 `project-handdrawn`。
- 项目3：约第 749 行，`id="project3"`，class 有 `project-handdrawn`。

### 2. 在空白内容区添加内容

🔍 在代码中搜索：`在此粘贴 Figma`

当前内容：

```html
<div class="section-content">
    <!-- 🔧 在此粘贴 Figma 导出的 HTML 代码，或自由排版 -->
</div>
```

你可以改成：

```html
<div class="section-content">
    <h2>《白蛇传》</h2>
    <p>这是一款基于中国民间故事改编的叙事解谜游戏。</p>
    <img src="images/project1-screenshot.jpg" alt="《白蛇传》游戏截图">
</div>
```

### 3. 使用 Figma 导出 HTML

🔍 在代码中搜索：`Figma 排版工作流`

推荐流程：

1. 在 Figma 建 900px 宽 Frame。
2. 把你的截图、标题、说明文字排好。
3. 用 Figma to HTML 插件导出。
4. 找到对应项目的 `.section-content`。
5. 把导出的 HTML 粘贴到注释位置。

示例：

```html
<div class="section-content">
    <!-- 把 Figma 导出的代码粘贴在这里 -->
</div>
```

### 4. 不用 Figma，手写简单图文排版

可以直接写：

```html
<div class="section-content">
    <h2>项目名称</h2>
    <p>一句话说明项目类型、平台和你的职责。</p>
    <img src="images/project2-main.jpg" alt="项目2主视觉">
    <p>补充说明：玩法、设计目标、迭代过程、测试反馈。</p>
</div>
```

如果想左右排版，可以加一个 class：

```html
<div class="section-content">
    <div class="project-layout">
        <img src="images/project2-main.jpg" alt="项目截图">
        <div>
            <h2>项目名称</h2>
            <p>项目介绍文字。</p>
        </div>
    </div>
</div>
```

再在 `<style>` 里加：

```css
.project-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    align-items: center;
}
```

### 5. 项目背景图更换

项目1：

🔍 在代码中搜索：`项目1 水墨背景图`

```css
.project-shuimo .section-bg {
    background-image:
        linear-gradient(rgba(248, 242, 223, 0.45), rgba(248, 242, 223, 0.78)),
        url("images/project1-bg.jpg");
}
```

项目2：

🔍 在代码中搜索：`项目2/3 背景图`

```css
.project-handdrawn .section-bg {
    background-image:
        linear-gradient(rgba(254, 249, 239, 0.55), rgba(254, 249, 239, 0.82)),
        url("images/project2-bg.jpg");
}
```

项目3：

🔍 在代码中搜索：`项目3 背景图`

```css
#project3 .section-bg {
    background-image:
        linear-gradient(rgba(254, 249, 239, 0.55), rgba(254, 249, 239, 0.82)),
        url("images/project3-bg.jpg");
}
```

### 6. 水墨风和手绘风在哪里改

项目1水墨风：

🔍 在代码中搜索：`项目1：水墨风格专属预留`

主要改这些：

```css
.project-shuimo {
    background: var(--bg-rice-paper);
}

.project-shuimo .section-content {
    font-family: var(--font-calligraphy);
}
```

项目2/3手绘风：

🔍 在代码中搜索：`项目2/项目3：柔和手绘风格专属预留`

主要改：

```css
.project-handdrawn .section-bg {
    background-image: ...;
}
```

## 七、个人简介板块

### 1. 替换头像

🔍 在代码中搜索：`个人头像`

把注释和文字换成：

```html
<img src="images/avatar.jpg" alt="你的头像">
```

### 2. 修改个人陈述

🔍 在代码中搜索：`个人陈述第 1 段`

每段都有中文和英文。中文改 `.zh`，英文改 `.en`。

```html
<p class="zh">这里写中文个人陈述。</p>
<p class="en">Write the English statement here.</p>
```

### 3. 技能标签增删改

🔍 在代码中搜索：`<ul class="skills">`

修改一个技能：

```html
<li>Unity</li>
```

改成：

```html
<li>Godot</li>
```

添加一个技能：

```html
<li>Playtesting</li>
```

删除技能：删掉整行 `<li>...</li>`。

### 4. 经历时间线添加条目

🔍 在代码中搜索：`经历 3`

复制一个完整的 `timeline-item`：

```html
<article class="timeline-item">
    <div class="timeline-date">2024</div>
    <div>
        <strong>经历标题</strong>
        <p>经历描述。</p>
    </div>
</article>
```

粘贴后改年份、标题、描述。

### 5. 修改联系方式

🔍 在代码中搜索：`contact-list`

邮箱：

```html
<a class="contact-link" href="mailto:your-email@example.com"><span class="icon">✉</span> your-email@example.com</a>
```

改成：

```html
<a class="contact-link" href="mailto:xiaobai@email.com"><span class="icon">✉</span> xiaobai@email.com</a>
```

GitHub：

```html
<a class="contact-link" href="#" target="_blank" rel="noreferrer"><span class="icon">⌘</span> GitHub</a>
```

改成：

```html
<a class="contact-link" href="https://github.com/yourname" target="_blank" rel="noreferrer"><span class="icon">⌘</span> GitHub</a>
```

## 八、全局样式调整

### 1. 修改主色调

🔍 在代码中搜索：`:root`

颜色都在约第 15 行开始：

```css
:root {
    --bg-cream: #fef9ef;
    --accent-pink: #f2a7b0;
}
```

比如想更偏浅蓝：

```css
:root {
    --bg-cream: #f8fbff;
    --accent-pink: #a9d6e5;
}
```

### 2. 更换字体

🔍 在代码中搜索：`Google Fonts`

引入字体在 `<head>` 里：

```html
<link href="https://fonts.googleapis.com/css2?family=Ma+Shan+Zheng&family=Varela+Round&family=ZCOOL+KuaiLe&display=swap" rel="stylesheet">
```

CSS 变量在 `:root`：

```css
--font-title: "ZCOOL KuaiLe", "Ma Shan Zheng", cursive;
--font-body: "Varela Round", "Microsoft YaHei UI", sans-serif;
```

如果你换了 Google Fonts 字体，也要把这里的字体名改掉。

### 3. 调整内容区宽度

🔍 在代码中搜索：`--content-width`

当前：

```css
--content-width: 900px;
```

如果你 Figma 设计稿是 1000px 宽：

```css
--content-width: 1000px;
```

注意：太宽会影响笔记本电脑上的阅读体验。申请作品集推荐 900-1000px。

### 4. 修改按钮圆角和阴影

🔍 在代码中搜索：`--radius-large`

圆角：

```css
--radius-large: 28px;
--radius-medium: 20px;
```

阴影：

```css
--shadow-soft: 0 16px 38px rgba(74, 58, 87, 0.14);
--shadow-small: 0 8px 18px rgba(74, 58, 87, 0.12);
```

更柔和：

```css
--shadow-soft: 0 10px 26px rgba(74, 58, 87, 0.10);
--shadow-small: 0 5px 14px rgba(74, 58, 87, 0.10);
```

## 九、测试与预览

### 1. 本地浏览器预览

最简单方法：

1. 打开文件夹 `MyGamePortfolio`。
2. 双击 `index.html`。
3. 浏览器会打开网页。

或者在 VS Code 里安装 Live Server 插件，右键 `index.html`，选择 `Open with Live Server`。

### 2. 用 F12 调试

在浏览器页面按 `F12`。

- `Elements`：看 HTML 结构。
- `Styles`：看 CSS 样式。
- `Console`：看 JavaScript 有没有报错。

如果按钮没反应，先看 `Console` 是否有红色报错。

### 3. 模拟手机屏幕

1. 按 `F12`。
2. 点击左上角像手机/平板的小图标。
3. 选择 iPhone 或 Pixel 尺寸。
4. 检查导航是否变成汉堡菜单。

### 4. 常见问题排查

图片不显示：

- 检查路径是否正确，比如 `images/avatar.jpg` 是否真的存在。
- 检查文件名大小写是否一致。`Avatar.jpg` 和 `avatar.jpg` 在 GitHub Pages 上可能不是同一个文件。
- 路径要用 `/`，不要用 Windows 的 `\`。

错误示例：

```html
<img src="images\avatar.jpg">
```

正确示例：

```html
<img src="images/avatar.jpg">
```

中文乱码：

- 确认文件顶部有：

```html
<meta charset="UTF-8">
```

- VS Code 右下角编码建议显示 UTF-8。

样式错乱：

- 检查是否漏了 `</div>`。
- 如果你粘贴 Figma HTML 后错乱，先撤销，然后一次只粘贴一个小块测试。

语言切换后内容缺失：

- 检查中文是否有 `class="zh"`。
- 检查英文是否有 `class="en"`。

## 十、发布到 GitHub Pages

### 1. 第一次上传步骤

推荐最简单流程：

1. 登录 GitHub。
2. 新建仓库，例如 `MyGamePortfolio`。
3. 上传这些内容：

```text
index.html
images/
agent.md
填空指南.md
```

4. 进入仓库 `Settings`。
5. 找到 `Pages`。
6. Source 选择 `Deploy from a branch`。
7. Branch 选择 `main`，文件夹选择 `/root`。
8. 保存后等 1-3 分钟，GitHub 会给你一个网址。

### 2. 更新已发布网站

以后你改了内容：

1. 修改本地 `index.html` 或 `images/`。
2. 把新文件上传到 GitHub 仓库覆盖旧文件。
3. 等 GitHub Pages 自动更新。

注意：如果图片名字变了，代码里的路径也要同步修改。

例如图片从：

```html
<img src="images/avatar.jpg">
```

改成了：

```html
<img src="images/profile-photo.jpg">
```

那 `images/` 文件夹里也必须真的有 `profile-photo.jpg`。
